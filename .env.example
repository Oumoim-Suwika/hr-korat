# This app runs fully in the browser (React + Vite) and stores data in
# localStorage. No external API keys are required to run it locally.
#
# Add environment variables below only if you introduce features that need them.
